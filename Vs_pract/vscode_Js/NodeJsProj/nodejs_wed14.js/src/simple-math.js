// function plus(x, y) {
//   return x + y;
// }
// function minus(x, y) {
//   return x - y;
// }
// function multiple(x, y) {
//   return x * y;
// }
// function divide(x, y) {
//   return x / y;
// }

module.exports = {
  divide: (x, y) => {
    return x / y;
  },
  multiple: (x, y) => {
    return x * y;
  },
  minus: (x, y) => {
    return x - y;
  },
  plus: (x, y) => {
    return x + y;
  },
};
